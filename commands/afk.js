exports.run = async(clinet, message, args) => {
  {
     {
 message.guild.member(message.author).setNickname(`[AFK] ${message.author.username}`)
 message.channel.send(`I have changed your status. Type **+back** if you not AFK anymore`)
}
}
}